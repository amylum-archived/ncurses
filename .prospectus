item do
  expected do
    url_xpath
    url 'https://www.archlinux.org/packages/core/x86_64/ncurses/'
    xpath '//*[@id="pkgdetails"]/h2'
    regex /^ncurses ([\d.]+)(\+)?(\d+)?-\d+$/, '\1-\3'
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
